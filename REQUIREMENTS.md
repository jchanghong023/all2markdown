# all2markdown 需求说明

## 文档定位

本文是 all2markdown 项目的**需求权威文档**。项目类型为自有项目（GitHub `jchanghong023/all2markdown`，仓库无上游 Fork 关系），因此不维护 `FORK.md`。开发规则、目录职责、入口命令与测试执行方式见 `AGENTS.md`。

条目来源标记：

- **【已确认】**：来自既有项目约束（`AGENTS.md`）、`README.md`、`THIRD_PARTY_NOTICES.md`、`docs/internal-corpus-structure.md` 或用户既有决定。
- **【依据现状恢复】**：从当前实现与公开行为反推，用于补齐缺失的需求描述，尚未经用户逐条确认。

实现状态与验证状态统一在第 6 节说明，二者不混同：功能存在不等于已验收。

---

## 1. 项目目标

1.1 【已确认】提供可在 Windows 11 x64、纯 CPU、无网络条件下批量运行的文档转换工具，把目录中的原始文件转换为 Markdown。

1.2 【已确认】转换结果用于后续阅读与检索，用户不应为使用该工具而阅读源码、手工比对或人工回归；交付质量以自动化测试为准。

1.3 【已确认】仓库可开源分发，因此仓库内不得包含私人文档、内部文件名或历史转换产物。

## 2. 主要用户场景

| 编号 | 场景 | 期望结果 |
| --- | --- | --- |
| S1 | 首次安装（可联网） | 运行 `init.cmd` 或双击 `gui.cmd` 后一键初始化，得到可离线转换的完整环境 |
| S2 | 图形界面批量转换（推荐） | 双击 `gui.cmd`，界面显示环境状态，选择输入目录后点「开始转换」，在界面内看到进度与日志 |
| S3 | 命令行/批处理转换 | `all2markdown.cmd <输入目录> [输出目录] [选项]` 可脚本化调用，退出码可判断结果 |
| S4 | 中断后重跑 | 已生成 Markdown 的文件被自动跳过，未完成部分继续转换，不重复劳动 |
| S5 | 音视频转写 | `.mp4` / `.m4a` 输入得到带时间戳的中文转录 Markdown |
| S6 | 失败定位 | 单个文件失败不影响批次；能从日志与退出码判断失败文件与原因 |

## 3. 范围边界

3.1 【已确认】项目只做「原始文件 → Markdown」的转换，不包含主题合并、私人文档管理与历史转换产物管理。

3.2 【已确认】文档解析、OCR、Layout/Table 识别、嵌入对象递归一律由固定版本的 Xberg 完成；本项目内不得自行实现 Office/PDF 解析器。

3.3 【已确认】`.mp4` / `.m4a` 走项目内的本地媒体转录链路；`src/convert_mp4.py` 是内部实现模块，不是第二个产品入口。

3.4 【已确认】每个顶层输入只生成一份 Markdown，嵌入内容合并进同一文件。

3.5 【已确认】不生成图片文件、不写入 Base64；但必须保留 Xberg 输出的图片路径引用（包括没有真实文件的占位路径），用于标记原文图片位置。

## 4. 功能需求

### FR-1 安装与初始化

- 【已确认】`init.cmd` 是唯一安装入口，允许联网下载托管 Python、锁定依赖与非 Python 固定资产。
- 【已确认】初始化可使用系统预装的 Windows x64 Python 3.8+；产品执行固定使用 uv 管理的 Python 3.12 与项目 `.venv`。
- 【已确认】初始化必须可重复执行、可从中断恢复；已下载且校验通过的资产在重跑时保留，失败下载不得覆盖已有的最终文件。
- 【已确认】所有非 Python 固定资产由 `src/config/install_assets.json` 唯一声明，安装时校验 SHA-256，并记录实际解析到的版本、URL、大小与摘要。
- 【已确认】显式配置资产镜像时，镜像失败必须明确报错，不得静默回落到官方源。

**验收条件**

1. 全新机器上执行 `init.cmd` 成功后，`.venv`、Xberg 运行时与模型资产齐备，`gui.cmd` 显示环境就绪。
2. 中断后重新执行 `init.cmd` 能继续完成，且不会破坏已校验通过的资产。
3. 人为破坏某个资产（删除或改大小）后，转换预检必须失败并提示重新运行 `init.cmd`，不得带病转换。

### FR-2 转换入口

- 【已确认】图形界面入口是 `gui.cmd`（未初始化时使用系统 Python 3.8+ 启动引导界面，已初始化时使用项目 `.venv`）。
- 【已确认】命令行转换入口是 `all2markdown.cmd`；未初始化时提示「尚未初始化，请先运行 init.cmd」。
- 【依据现状恢复】命令行参数：`input_dir`、`output_dir` 为可选位置参数，省略时使用仓库内 `input/`、`output/`；`--flat` 平铺输出；`--exts` 按扩展名过滤；`--timeout` 覆盖单文件超时；`--xberg-config` 替换管线配置文件。
- 【依据现状恢复】图形界面固定以平铺方式输出到仓库内 `output/`，并按文件类型分组提供多选过滤。

**验收条件**

1. `gui.cmd` 与 `all2markdown.cmd` 在未初始化与已初始化两种状态下都给出明确结果，不出现无提示失败。
2. 省略目录参数时使用默认 `input/`、`output/`，并明确告知用户实际使用的目录。

### FR-3 扫描与类型过滤

- 【已确认】递归扫描输入目录中的所有普通文件；输入目录只读，任何情况下不得修改或删除输入。
- 【依据现状恢复】支持范围以固定版本 Xberg 的格式清单为准（`.pdf`、`.docx`、`.pptx`、`.xlsx` 强制纳入），并按扩展名与所选类型求交集；`.mp4` / `.m4a` 由本地媒体链路处理。
- 【依据现状恢复】过滤只做减法：未选中的类型连扫描都不进入；扩展名过滤不做有效性校验（拼写错误最多匹配不到文件）。
- 【依据现状恢复】扫描会跳过输出目录自身，避免把输出当成新输入。

**验收条件**

1. 子目录层级被完整遍历，输出目录不会被再次纳入扫描。
2. 选择部分类型时，只有该类型文件进入转换流程。

### FR-4 输出组织

- 【已确认】输出保持输入目录层级，文件名保留原扩展名并转为小写后缀（`a.docx` → `a_docx.md`，`a.mp4` → `a_mp4.md`）。
- 【依据现状恢复】`--flat`（图形界面固定使用）忽略输入目录层级，不同子目录中的同名文件共享一个输出命名空间：同一批次内只转换排序最前的一个，其余跳过并记录日志，已有输出时也不覆盖。
- 【已确认】每个顶层输入只产生一个 Markdown；嵌入文档、嵌入表格与图片 OCR 文本合并到该文件内。
- 【依据现状恢复】嵌入子文档以 `## Embedded document: <显示名>` 分节追加，相同内容只保留一次；超出嵌套深度上限的子项不再展开。

**验收条件**

1. 同一输入目录重复转换的产物路径稳定、可预期。
2. 平铺模式下同名文件不会互相覆盖，也不产生不可区分的结果。

### FR-5 跳过已有结果

- 【已确认】当对应 Markdown 已存在时直接跳过；不删除、不覆盖已有文件。
- 【已确认】转换过程不生成额外的进度文件或状态文件。

**验收条件**

1. 二次运行同一输入目录时，所有输出文件的修改时间保持不变。
2. 跳过数量与原因可在日志中看到。

### FR-6 文档转换

- 【已确认】文档解析、OCR、Layout/Table 识别与嵌入对象递归全部交给初始化时安装的固定版本 Xberg 完成。
- 【已确认】转换使用项目内固定配置（`src/config/xberg_offline.json`）：Markdown 输出、中文 OCR、CPU execution provider、启用 Layout 与表格识别。
- 【已确认】图片处理遵循 3.5：保留图片路径引用以标记图片位置，不落地图片文件、不写 Base64。
- 【依据现状恢复】扫描页与图片中的文字通过 OCR 进入 Markdown；无法识别到文字与图片无法解码属于不同情况，需要在日志中可区分。

**验收条件**

1. 原生文本、表格、字段、嵌入文档在转换后仍出现在同一份 Markdown 中，内容不丢失。
2. 输出 Markdown 中不出现 Base64 数据块，也不产生图片文件；图片位置以引用/占位路径体现。
3. 单个图片或单个嵌入对象失败不影响该顶层文档的其余内容输出。

### FR-7 媒体转录

- 【已确认】`.mp4` / `.m4a` 固定走本地链路（PyAV 解码 → Silero VAD → SenseVoice INT8），全程离线、CPU 推理；不提供其他媒体转录后端。
- 【已确认】配置与代码中现有的 `media_transcription_backend` 切换分支（`"xberg"` 路径）属于历史遗留，不属于需求，应在后续开发中移除（见第 8 节）。
- 【已确认】中文识别、ITN 与 Silero VAD 行为固定，不得因打包或重构而变化。
- 【依据现状恢复】输出 Markdown 包含音频时长、语音片段数与逐段转录文本（含起止时间戳）；无音轨的输入给出明确说明而不是空文件。

**验收条件**

1. 固定中文语音样本转换后包含预期文本与时间戳信息。
2. 无音轨或解码失败的输入被明确报告，且不产生半成品输出。

### FR-8 大文档分流

- 【依据现状恢复】对 PDF、Word 家族与 PowerPoint 家族先做仅读元数据的页数/幻灯片数判断；超过阈值（当前默认 200）时使用更快的提取配置，跳过高开销的 Layout 与图片 OCR。
- 【依据现状恢复】无法取得页数的格式（如 XLSX）与未安装页数探测依赖时，一律使用常规配置，不得因为判断失败而丢内容。
- 【已确认】分流策略属于产品行为，必须在日志中体现（哪个文件走了哪种模式）。

**验收条件**

1. 超过阈值的文档能在明显更短时间内完成，并在日志中标注分流模式。
2. 未超阈值或无法判断的文档输出质量与常规路径一致。

### FR-9 失败隔离与原子落盘

- 【已确认】单个文件失败不得终止整个批次，失败信息记入日志与最终统计。
- 【已确认】失败不得留下不完整的最终 Markdown：先写临时文件，成功后再原子替换。
- 【已确认】项目临时文件统一写入仓库 `.tmp/` 目录。
- 【依据现状恢复】服务不可用、预检失败等批次级问题与单文件失败区别对待，返回不同的退出码。

**验收条件**

1. 输入目录中混入损坏文件时，其余文件仍全部转换成功，退出码体现「部分失败」。
2. 转换中断后输出目录中不存在非 Markdown 的残留文件，也不存在内容截断的 Markdown。

### FR-10 过程可见性与停止

- 【依据现状恢复】转换过程输出可解析的进度信息：待转换总数、逐文件开始/完成/失败、成功与失败计数、总耗时、超过心跳间隔时的进行中提示。
- 【依据现状恢复】图形界面复用同一份进度与日志输出，不单独实现第二套状态来源。
- 【依据现状恢复】用户可请求停止：停止粒度是「当前文件完成后不再取新文件」，并返回可识别的退出码。
- 【依据现状恢复】长时间运行（大量文件、大量日志）时界面保持可用，不因日志无限增长而卡死。

**验收条件**

1. 界面上进度条与计数随转换推进，失败文件在日志中可见。
2. 转换中点击停止后，当前文件完成即停止，已完成结果保持可读。

### FR-11 环境状态检测

1. 【依据现状恢复】图形界面在启动时检测并展示：系统平台、初始化解释器、项目环境、运行依赖、Xberg 运行时、媒体模型、CPU 指令集支持；只有必需项全部就绪才显示「就绪」。
2. 【依据现状恢复】未就绪时提供「一键初始化」入口，初始化过程中展示进度与日志，完成后可用项目环境重新打开界面。
3. 【依据现状恢复】初始化失败时明确提示返回码与「已通过的资产会保留、可稍后重试」。
4. 【已确认】界面的具体呈现（窗口尺寸、字体、配色、控件布局、日志行数上限等）不构成需求，由 Agent 在保证功能可见与可读的前提下自行优化；本节验收只针对功能是否可见、可操作。

**验收条件**

1. 缺失任意一项必需组件时，界面明确指出缺什么，而不是在转换时才失败。
2. 一键初始化成功后，界面自动刷新为就绪状态。

### FR-12 退出码

【依据现状恢复】退出码含义：`0` 成功、`1` 部分失败、`2` 用法或配置错误、`3` 预检失败、`4` 服务启动失败、`11` 未预期错误、`130` 用户取消。其中 `4` 与 `130` 只适用于文档转换主入口；媒体模块单独运行时使用同义的 `0`/`1`/`2`/`3`/`11`，不返回 `4` 与 `130`。

**验收条件**

1. 上述每种情况都能通过一次真实调用复现出对应退出码。

---

## 5. 跨功能约束

### 5.1 平台与兼容性

- 【已确认】仅支持 Windows 11 x64、纯 CPU、支持 AVX2 的处理器。
- 【已确认】安装代码必须兼容 Python 3.8 标准库；转换代码运行于托管 Python 3.12 与项目 `.venv`。
- 【已确认】不得改变既有 Xberg 配置、Fast/Normal 分流、嵌入递归、媒体转录、跳过、错误隔离与原子输出行为；打包或安装类改动以最小改动为原则。

### 5.2 离线与运行环境

- 【已确认】转换阶段强制离线：不得联网补下载，也不得读取无关的用户历史缓存；模型与运行时必须来自初始化时安装的固定资产。
- 【已确认】Xberg 的 Hugging Face 缓存指向安装包内的模型目录，并使用 CPU execution provider。

### 5.3 依赖与资产固定

- 【已确认】Python 依赖只记录在 `requirements.txt` 与 `requirements-dev.txt`，版本全部使用 `==` 锁定；媒体链路固定 sherpa-onnx 1.13.6、PyAV 18.1.0、NumPy 2.5.2。
- 【已确认】不得使用 Git LFS，不得提交运行时、模型、wheel、Python 解释器、下载缓存或安装产物。
- 【已确认】第三方组件的版本、许可与来源记录在 `THIRD_PARTY_NOTICES.md` 与 `licenses/third_party/`；分发时必须满足对应上游许可与 notice 要求。
- 【已确认】本项目为个人项目，无需提供项目自身的许可证文件，也不为此新建 `LICENSE`。

### 5.4 数据处理与保密

- 【已确认】输入目录只读；仓库与公开测试不得包含私人文档、内部文件名、正文、OCR 文本或从内部文档生成的 Markdown。
- 【已确认】内部语料只允许本地只读验证，公开测试必须使用从零构造的合成夹具。
- 【已确认】测试、文档与公开问题中不得记录内部文件名。

### 5.5 性能约束

- 【依据现状恢复】纯 CPU 管线下单文件请求独占全部线程预算，客户端并发请求数固定为 1；不得用多请求并发换取吞吐（实现注释记录该做法会更慢并可能超时）。
- 【依据现状恢复】大文档通过 FR-8 的分流而不是提高并发来保证可完成性。

---

## 6. 实现与验证状态

状态口径：**已实现**指代码路径存在；**已验证**指存在可在当前环境复现的自动化验证；两者独立。

| 需求 | 实现 | 自动化验证现状 |
| --- | --- | --- |
| FR-1 安装与初始化 | 已实现（`init.cmd`、`src/init_env.py`） | 资产下载/续传/校验/镜像与 bootstrap 有单元测试；`init.cmd` 的完整流程仅在 CI 中执行 |
| FR-2 转换入口 | 已实现（`gui.cmd`、`all2markdown.cmd`） | 入口脚本拒绝未初始化环境的契约有单元测试；GUI 启动本身依赖图形环境 |
| FR-3 扫描与类型过滤 | 已实现 | 类型过滤、平铺去重有单元测试；过滤与真实格式清单的集成测试已存在但依赖已安装资产 |
| FR-4 输出组织 | 已实现 | 输出命名、嵌入合并、平铺路径有单元测试；真实嵌入文档合并有集成测试（依赖资产） |
| FR-5 跳过已有结果 | 已实现 | 集成测试验证二次运行不重写输出（依赖资产） |
| FR-6 文档转换 | 已实现（Xberg + 固定配置） | OCR、表格、扫描页、嵌入递归等集成测试存在但依赖已安装资产；覆盖范围远小于第 7 节目标矩阵 |
| FR-7 媒体转录 | 本地链路已实现（`src/convert_mp4.py`）；`media_transcription_backend` 切换分支尚未移除（见第 8 节） | 真实中文语音集成测试存在但依赖已安装模型；Markdown 结构、时间戳格式、术语替换有单元测试 |
| FR-8 大文档分流 | 已实现 | 阈值边界与管线配置拆分有单元测试；端到端分流有集成测试（依赖资产与 PyMuPDF） |
| FR-9 失败隔离与原子落盘 | 已实现 | 坏文件隔离与部分失败退出码有集成测试；原子落盘无直接单元测试，由集成测试间接覆盖（失败不产生半成品、二次运行不重写输出） |
| FR-10 过程可见性与停止 | 已实现（含 GUI 解析） | 进度解析、停止信号、返回码文案有单元测试；取消有集成测试（依赖资产） |
| FR-11 环境状态检测 | 已实现（`src/gui_status.py`） | 状态项与命令构造有单元测试；界面交互本身无自动化验证 |
| FR-12 退出码 | 已实现 | 用法错误、预检失败、部分失败、取消等分支有测试覆盖；服务启动失败（4）与未预期错误（11）分支无自动化验证 |
| 5.4 保密约束 | 已实现（夹具合成脚本、`.gitignore`） | 无自动检查；依赖评审 |

最近一次文档任务中实际执行的验证（2026-09-16，过程记录、仓库内无留痕）：部分单元测试类通过；`fulltest.py --help` 可正常输出。**完整测试套件（含真实 Xberg 转换与 79 秒语音转写）未在本次文档任务中执行**，其执行入口与条件见 `AGENTS.md`。

---

## 7. 已确认但尚未验证的覆盖目标

`docs/internal-corpus-structure.md` 给出了公开回归夹具的最低测试矩阵（26 项）与验证层次，属于项目内已确认的覆盖目标，明细以该文档为准。与当前自动化测试对照，主要缺口：

1. **矢量与特殊图片**：EMF/WMF（含 standard-header WMF）、SVG 的独立覆盖；JPEG 2000、JBIG2、PNM 家族与 BMP/WebP/GIF/TIFF 独立图片输入。
2. **Office 家族变体**：DOCM/DOTX/DOTM、PPTM/PPSX/POTX/POTM、XLTX/XLSM/XLSB/XLAM 与 OpenDocument 的入口、嵌入对象与页数行为。
3. **字段与图片身份**：PowerPoint `a:fld` 字段值不得丢失；图片与 OCR 结果的稳定身份关联（XML 顺序 ≠ 版面顺序）；同一媒体多 shape 复用时的位置保留。
4. **OLE 与预览图**：OLE 本体失败与 preview OCR 成功必须分别报告，不得互相冒充。
5. **媒体链**：`.mp4` / `.m4a` 的错误隔离与损坏文件不落盘已有基本覆盖；其余媒体容器格式与更多损坏变体（截断文件、损坏音轨等）仍待补齐。
6. **运行时其他格式**：固定 Xberg 格式清单的最小烟测，以及「最终 Markdown 不含 Base64 或图片文件」的全局断言。
7. **界面主流程**：开始/停止/一键初始化/线程间的交互无自动化验证，无显示环境下界面相关测试会跳过。
8. **未接入回归的脚本**：`fulltest.py` 是手动执行的集成验收脚本，未在 CI 中运行。

上述缺口属于「已确认目标、尚未验证」，不得据此认定功能已验收，也不得为了让文档符合现状而删除这些目标。

---

## 8. 已确认但尚未实现的变更

以下变更已由用户确认，但本轮只更新文档，未改动代码与测试文件：

1. **媒体转录固定为本地链路**：移除 `media_transcription_backend` 配置项及 `"xberg"` 代码路径（见 FR-7）。
2. **删除未引用夹具**：删除 `tests/test_example/` 中没有被任何测试引用的样本文件：`1706.03762.pdf`、`58325_db.xlsx`、`bug62513.pptx`、`chartex.docx`、`mixed_native_scanned.pdf`、`sample_with_images.docx`。

这两项需在后续开发任务中完成，并按 `AGENTS.md` 的要求补充相应验证。

## 9. 待确认问题

1. **第 7 节缺口优先级**：哪些覆盖缺口需要在下一个迭代内补齐，需要用户确认排期。
